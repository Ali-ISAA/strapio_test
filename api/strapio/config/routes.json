{
  "routes": [
    {
      "method": "GET",
      "path": "/strapios",
      "handler": "strapio.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/strapios/count",
      "handler": "strapio.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/strapios/:id",
      "handler": "strapio.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/strapios",
      "handler": "strapio.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/strapios/:id",
      "handler": "strapio.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/strapios/:id",
      "handler": "strapio.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
